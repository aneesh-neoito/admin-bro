/// <reference types="react" />
import { ActionProps } from '../action.props';
declare type SubmitFormProps = {
    props: ActionProps;
    loading: boolean;
};
export declare const submitForm: ({ props, loading }: SubmitFormProps) => "" | JSX.Element;
export {};
